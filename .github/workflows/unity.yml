# Generated by internal/ci/ci_tool.cue; do not edit

name: Unity
"on":
  push:
    branches:
      - unity/*/*
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        go-version:
          - 1.19.x
        os:
          - ubuntu-20.04
    timeout-minutes: 15
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Install Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go-version }}
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true
      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-${{ matrix.go-version }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-${{ matrix.go-version }}-go-
      - name: Install unity
        run: ./_scripts/installUnity.sh
      - name: Run unity
        run: ./_scripts/runUnity.sh change:$(basename $(dirname $GITHUB_REF))/$(basename $GITHUB_REF)
