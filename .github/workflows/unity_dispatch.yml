# Generated by internal/ci/ci_tool.cue; do not edit

name: Dispatch unity
"on":
  - repository_dispatch
jobs:
  unity:
    runs-on: ubuntu-20.04
    defaults:
      run:
        shell: bash
    if: ${{ github.event.client_payload.type == 'unity' && github.event.client_payload.payload.cl != null}}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - id: gerrithub_ref
        run: |-
          ref="$(echo ${{github.event.client_payload.payload.ref}} | sed -E 's/^refs\/changes\/[0-9]+\/([0-9]+)\/([0-9]+).*/\1\/\2/')"
          echo "gerrithub_ref=$ref" >> $GITHUB_OUTPUT
      - name: Trigger unity
        run: |-
          git config user.name porcuepine
          git config user.email porcuepine@gmail.com
          git config http.https://github.com/.extraheader "AUTHORIZATION: basic $(echo -n porcuepine:${{ secrets.PORCUEPINE_GITHUB_PAT }} | base64)"
          git checkout -b unity/${{ github.event.client_payload.payload.cl.changeID }}/${{ github.event.client_payload.payload.cl.commit }}/${{ steps.gerrithub_ref.outputs.gerrithub_ref }}
          git push https://github.com/cue-unity/unity-trybot unity/${{ github.event.client_payload.payload.cl.changeID }}/${{ github.event.client_payload.payload.cl.commit }}/${{ steps.gerrithub_ref.outputs.gerrithub_ref }}
